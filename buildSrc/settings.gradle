rootProject.name = 'nats-parent'
dependencyResolutionManagement {
    dependencyResolutionManagement {
        versionCatalogs {
            libs {
                from(files("../gradle/libs.versions.toml"))
            }
        }
    }
}
